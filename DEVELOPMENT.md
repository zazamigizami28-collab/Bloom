# 花守り 開発ガイド — ver0.6.6

## 現在地
Phase 0〜4.5の練習場を継続。ユーザーがパリィ/BREAK・成長・リトライ・休止復帰を確認し、Phase 5へ進むことを承認。開花演出・20秒・従来操作をユーザー確認済み。花風の自動追撃、種へ戻る変更を反映し、現在はPhase 6の番人戦評価版。
TypeScript / Phaser 3.90 / Vite / Web Audio、PCキーボード・パッド対応を継続する。

## 再開と検証
既存チェックアウト・Sitesプロジェクトを継続する。依存関係がない場合のみ `npm ci`。
`npm run dev` で開発、`npm run check` で以下を順に実行する。

1. lint：TypeScript strict、未使用項目、独自ASTによる責務間依存チェック。
2. format:check：Prettier。
3. test：戦闘・演出イベント・状態リセット・入力/UI解除の回帰テスト。
4. build：型検査と Vite 配布物生成。

Lint は ESLint ではなく TypeScript + 独自の依存ルール。外部ツール導入は目的が必要な時に行う。
`dist` と `node_modules` は生成物。ソースとロックファイルをGitで管理する。

## 責務・編集箇所
| ファイル | 担当 |
|---|---|
| main.ts | Phaser初期化、Sceneリソース生成・破棄、更新と表示の接続 |
| game-state.ts | 状態の所有、Session/Player/Enemyの状態名、読み取り専用スナップショット |
| practice.ts | セッション進行、入力アクション、通常攻撃・専用一撃、命令・リセット |
| player-system.ts | 移動・ジャンプ |
| enemy-system.ts | 型付き攻撃タイムライン、予兆、攻撃解決、周期復帰 |
| special-system.ts | 飛び道具の接触、特殊パリィ、栄養取得 |
| flower-system.ts | 開花通知・開花時計・花風の発射と接触 |
| combat.ts / flower.ts | 判定・BREAKと水・肥料のモデル |
| actions.ts / input.ts | 入力契約とキーボード・パッド入力、解除 |
| commands.ts / ui.ts | UI操作契約、DOM配線・解除、状態からフォーム表示 |
| events.ts / event-presenter.ts | 型付きイベント契約と音・粒子・揺れへの変換 |
| scene-view.ts / render.ts | スナップショットから描画 |
| effects.ts / audio.ts / motion.ts / presentation.ts | 火花・SE・姿勢・演出定数 |
| data.ts | ゲーム調整値と敵AttackEventデータ |

## 更新順序
入力取得 → 休止/ヒットストップ → ゲーム時計 → リトライ/敵復帰 → 専用一撃 → BREAK → パリィ受付 → 攻撃開始 → 移動 → 命中 → 敵攻撃 → 特殊弾 → 周期終了 → イベント消費 → コピー状態を描画。
この順序を変更するとパリィ、BREAK、専用一撃が変わるので回帰テストを伴わせる。

## 状態の扱い
SessionStatus: ready / running / paused / dead。
PlayerAction: idle / parry / attack / finisher。
EnemyStatus: active / broken / defeated。
状態名は既存タイムスタンプから導出し、二重管理を避ける。完全な状態マシンへの置換は未実施。
モデルは状態を所有し、UIにはコピーのみを渡す。命令は Practice.command で処理する。
イベントはフレーム内に順番を保ってdrainし、一度だけ再生する。reset時は残るイベントを捨てclearを発行する。

## 完了判定
自動ゲートとユーザー評価を分ける。ブラウザ動作、聴感、物理パッド、演出理解は未確認なら必ず残件とする。
基準v0.5の原文ファイルは取得できず、過去会話から確認できた方針をAGENTSと本書へ反映した。原文の全条項との逐条照合済みとはしない。
次は番人戦の評価。その後に サブ花 → 拠点 → 探索 → 縦切り完成の順を維持する。


## Phase 5：ひまわり
- 水3回で自動開花。20秒間、通常攻撃と専用一撃に+4ダメージ。肥料加算は従来通り。
- 開花中の水は残り時間+2秒（残り最大20秒）。満タンでも特殊パリィの成功・成長報酬は維持（v0.6.4から遠距離パリィのBREAK蓄積なし）。
- 終了時は種を鉢へ落とし、種（水0）へ戻る。肥料を維持し、水3回で再開花。
- 被弾では開花を失わない。死亡リトライ/全リセットでは花と花風を初期化。
- 通常攻撃の開始時に花風を自動追加。専用キーは廃止。開花中かつ再使用可能な時だけ発射し、攻撃受付に失敗した入力では発射しない。
- 花風は移動やパリィをロックせず、射程340・速度620・判定半径18、再使用2.4秒。
- 花風は発射時の攻撃力を保存（14+開花4+肥料）、単発命中でBREAK+8。撃破時には蓄積しない。
- 発射済み花風は開花終了後も飛び、射程末端で消える。入力長押しでは連射しない。
- 開花と再使用時間はゲーム時計。休止・ヒットストップ・死亡待ち・練習機再起動中は停止。低速設定に従う。ボスの形態移行中も停止する。
- 開発メニューの成長「開花（20秒）」で直接試せる。通常経路と同じ開花音/花びら通知を発行する。
- flower.tsは状態遷移、flower-system.tsはゲーム接続、presentation/effects/audio/renderは表示/音。Sceneへ分岐を追加しない。


## Phase 6：園芸管理機 HRT-01
- 初期画面は番人戦。「練習場へ」で旧練習場を選べる。切替時はHP・花・弾・時計依存の行動をリセット。
- 園芸管理機 HRT-01。高さ295（主人公59の5倍）、幅150。HP650、半分で過給運転。予備動作前半に速度82で間合い145へ近づき、55%以降は停止して予兆方向を固定する。
- 前半：給水パルス1発 → 剪定二連 → 施肥/剪定 → 剪定二連。
- 後半：過給給水1発 → 剪定三連 → 土壌転圧 → 施肥/剪定 → 剪定三連。
- 地面攻撃：橙の帯と低音で予告。パリィ不可、ジャンプで避ける。射程430、高さ28、1ダメージ。
- phase移行は1.4秒。敵味方の攻撃・弾を切り、移行中は無敵。終了後に予備動作から始める。
- BREAK時間切れ/専用一撃後は次の攻撃へ。散水の再開だけが続くことを防ぐ。
- 撃破は結果画面を維持。「もう一度挑む」で再戦。死亡は従来の短い間を置いて全快・種状態から再挑戦。
- boss.tsは攻撃順と形態、boss-view.tsは専用描画。既存enemy-systemは共有する接触/パリィ計算。
- 後半・左右接触・ジャンプ回避・遷移清掃・勝利固定・再挑戦をtests/boss.mjsで確認する。
- ボス戦の所要時間・難易度・攻撃予告の読みやすさは実プレイ評価待ち。


## v0.6.2 ボス戦の改修（上の旧Phase6記録より優先）
- W/↑・パッドAでジャンプ。Space・パッドBでローリング。
- ローリングは地上のみ、420ms・速度650、開始から750msで再使用可能。方向入力があればその方向、なければ向いている方向。攻撃/パリィを中断し、ローリング中は新規攻撃/パリィ/ジャンプを受け付けない。専用一撃中は受付不可。
- ローリング中だけ本体と攻撃を通過。終了時に本体内なら近い側の外へ解決。移動とジャンプでは本体を通過しない。壁際にも主人公の幅を確保する。
- 状態はgame-state、開始/リセットはpractice、移動と本体接触はplayer-system。
- 間合い145→235、剪定射程175→300、高さ120。練習機の射程/操作感は維持。
- 前半: 圧縮弾→水→圧縮弾 / 伸縮剪定二連 / 圧縮弾→肥料→圧縮弾 / 突進。
- 後半: 同じ交互射出（短い間隔）/剪定三連/土壌転圧/施肥混合/突進。
- 突進は予兆1150ms、速度560、最大620ms。方向固定、最初の接触だけ解決、パリィで停止。BREAK、死亡、休止、形態移行で進行を止める。
- 白い菱形の圧縮弾は通常パリィのみ（成長なし、通常28/ジャスト38 BREAK）。青い滴は水、金の四角は肥料。既存特殊弾速度を共通にし、射出間隔で交互の着弾を維持する。
- 特殊弾は移動区間で接触判定し、高速フレームでの飛び越しを防ぐ。
- 水1回で1段階、開花20秒、自動花風、終了時種への帰還は維持。
- 描画はboss-viewのノズル加圧、ホッパー振動、伸縮剪定、履帯突進に分離。回避と弾の形状はscene-view。
- 自動検証: tests/encounter.mjsを追加。実プレイでの難易度・見やすさ・聴感はユーザー評価待ち。


## v0.6.3 剪定とパリィ
剪定のAttackEventにshear（sweep/overhead/rising）を追加。横薙ぎ・頭上溜め・低い構えからの切り上げで準備姿勢と軌跡を分ける。shear-pose.tsを腕と発光の共通座標計算とし、boss-viewが描画する。連撃を等間隔から切り上げ720ms/頭上への1680msなどのリズムに変更、前後半の攻撃順へ複数の剪定パターンを追加。資源混合弾と突進は維持。
ボスの音/発光予告は160ms前（従来80ms）。剪定刃先は420ms前から収束環、160ms前に半径42の光と十字・刃全体発光。練習機は80msのまま。受付180ms/ジャスト60msは維持。予告で即入力も通常パリィ圏内。
通常パリィのボス反動36、ジャスト54。描画で腕を上後方へ押し返し胴を沈める。実座標は変えないので成功後に間合いがずれない。
ローリング速度を650から650×2/3へ、時間420msと再使用750msは維持。最後のフレームの移動量を残り時間で制限し、自由空間の移動距離182。密着から反対側まで通過できることを検証。


## v0.6.4 開始距離とBREAK（最新・旧数値より優先）
- Phase 7へ進まず、1ボス戦の完成度を調整する。v0.6.3の構え・予告・反動・攻撃別パリィ・ローリングはユーザー評価済み。
- ボス戦の主人公開始Xはdata.tsのboss.playerStartX=300。ボスX755との中心距離455（旧145）。開始前・開始・再戦・死亡復帰・モード切替で統一。練習場はX610を維持。
- 近接パリィBREAKを通常28→19、ジャスト38→25（約2/3に丸める）。剪定と突進、練習機に共通。減衰も他の攻撃もなければ通常6回/ジャスト4回で100。
- special-system.tsの圧縮弾・水・肥料のパリィからBREAK加算を除去。成功音・演出・入力復帰・水成長・肥料強化は維持。
- 通常攻撃+4、花風命中+8はパリィではないため維持。HP、ダメージ、受付時間、攻撃順は今回変更しない。
- 「難易度が低め」はユーザー所感として記録。今回のBREAK減少後の撃破時間・被弾・崩し頻度を評価してから次の難易度調整を判断する。


## v0.6.5 最新
AUDIT-0.6.5.mdの距離選択・背後制限・可変隙・第二形態追撃を優先する。背後反撃は行動境界で判断し、予備中は方向と位置を固定。資源枠を残す。Phase 7と回復は未実装。


## v0.6.6 最新
AUDIT-0.6.6.mdを優先。AttackEvent.backは本体の固定向きに対して反対側、表示と命中で共有。回復はhealAt/healsLeftで管理し、被弾解決の後で完了判定。Phase 7未着手。


## v0.6.7 最新
AUDIT-0.6.7.mdを優先。攻撃はv0.6.5基準、ボス肥料弾廃止、3/4段、水3発目、弾1.8倍、方向追従100ms前まで。回復維持。


## v0.6.8 最新
AUDIT-0.6.8.mdを優先。boss.tsのrhythmは攻撃別の命中時刻。近接の最終段を赤い攻撃とし、射出は通常弾/通常弾/水の評価済みテンポを保持。


## v0.6.9 最新
AUDIT-0.6.9.md優先。赤い攻撃は高枝最終段のみ、地面攻撃は選択肢から除去。短枝2段目460ms。

共通最終合図はMIN_TELEGRAPH_MS=160。練習機80→160、赤い回避音650→160。ボスの方向固定も100→160へ。地面攻撃では早い薄い予兆と160ms前の強い発光を分離。


## v0.6.10 最新
発光はtelegraph.tsを共有。開始-160ms、白化-80ms、攻撃0msで消灯。構え時間から発光を算出しない。


## v0.6.11 現行基準
HRT-01開発終了（ユーザー判断）、Phase 7未着手。光を480msへ修正し、白化は240ms前。音/方向固定は160ms前を維持し、最終合図の最低160msと受付180msは維持する。docs/HRT-01-RETROSPECTIVE.mdとdocs/BOSS-DEVELOPMENT-GUIDE.mdを次ボス着手前に読む。

## v0.7.0 現行基準
Phase 7評価版。AUDIT-0.7.0.md優先。ボス命中時enemyHitAt/35ms停止はhitDummyで共通化しresetで消去。telegraphの描画を1フレーム内で収集し粒子後にflush。feedbackイベントでBREAK/一撃の揺れをpresentationへ分離。回復healイベントは成長と独立。HUD Textは別カメラ、背景と戦闘描画だけ揺らす。

## v0.7.1 現行基準
開始コマンド/Startはbegin(true)で1200ms導入、低レベルbegin()は演出なしの初期化。敗北はdefeat()へ集約、900ms後の自動再戦は導入を繰り返さない。勝利結果は1100ms後。全表示はsnapshot/ゲーム時計から導出し休止に従う。文書索引docs/README.md。

## v0.8.0 現行基準
Phase 9評価版。locationはhub/path/gate/battleでmodeとは別。非戦闘はjourneyで更新しAI/戦闘処理へ入らない。home命令で戦闘状態を初期化、勝利後ならrestoredGardenを保持。開始画面は庭へ。既存beginは直接戦闘の初期化。ボス死亡リトライはbattle内を維持。画面リロード時の進行保存は未実装。
