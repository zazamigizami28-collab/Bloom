# v0.8.1 — 横スクロール導入
ユーザー指摘: 道中に基礎学習と弱敵が必要。Jの範囲が不明、拠点の浮いた種/住人表示、ローリング不可。

水路を2700pxへ拡張。追従表示は1024pxの表示幅でクランプ、HUDは固定。安全な移動→高さ38のジャンプ障害→ローリングで通過できる枠→HP20の小型整備機2体。通常攻撃10×2、被弾1、構え1000ms/通常休止1500ms。通常パリィで2400msの隙。光480/白240/音160、パリィ180/60は共通。倒さず回避して進むことも可能。
探索ローリングを開始処理から復旧。時間420ms/距離182/再使用750msを維持。
journeyTargetを調査とプロンプトに共有し、範囲内・地上・非ローリングで[J/X]表示。画面下ボタンも範囲外で無効。
頭上用plantを空中に単独配置していた箇所を削除。住人はpotとplantを組み合わせ、休息場所は地面の輪。
弱敵は既存練習機の形状を再利用し、向きに合わせて反転。探索では攻撃とパリィを追加。死亡は900ms後に庭へ。ボス戦は従来どおり直接再戦。
検証は既存全回帰に範囲表示/空中除外、探索ローリング、移動上限、弱敵パリィ/2振り撃破を追加。
実機での新道中の視認性・テンポは未検証。セッション保存のみ。道中の回復E、特殊弾・花成長連携は未実装（拠点休息/出撃時補充は既存）。背景は既存固定背景を再利用する。
