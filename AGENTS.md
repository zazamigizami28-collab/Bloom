# 花守り：継続実装ルール

着手前に DEVELOPMENT.md と最新 AUDIT を読む。現在は ver0.6.0 / Phase 6 の番人戦評価版。

- ユーザーの最新指示を優先する。Phase 4.5はユーザー評価済み。Phase 5は評価済み。花風自動追撃・種へ戻る修正とPhase 6は承認済み。サブ花や拠点は次の指示前に追加しない。
- Scene は生成、入力取得、更新呼出、イベント配信、描画、shutdown のみを担当する。
- 戦闘・花・状態は DOM / Phaser / AudioContext へ依存させない。
- 状態更新 → 型付きイベント → 演出、状態スナップショット → UI の方向を守る。
- UI は PracticeCommands のみで操作する。モデルや Scene のフィールドを書き換えない。
- 新しい状態は GameState と遷移処理・リセット条件をセットで設計する。
- 敵攻撃は AttackEvent（時刻と種類）で記述する。並列配列や未検証の型キャストを追加しない。
- 登録する入力・DOM・Sceneイベント・音声リソースは解除経路も同時に実装する。
- 調整値は data.ts、演出値は presentation.ts へ。受付時間・ダメージ変更は意図を明記する。
- 変更後は npm run check を通す。描画スタブや自動テストを実ブラウザ・聴感・実機パッド確認と呼ばない。
- 完了報告は変更内容、理由、検証済み、未検証、残件、ユーザー確認表を記す。
- 自動ゲート通過だけで Phase 完了としない。操作感・演出理解の評価を別に残す。
